<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_dark"
    android:fitsSystemWindows="true"
    android:orientation="vertical">

    <!-- Top Half: Video Player Container -->
    <RelativeLayout
        android:id="@+id/playerContainer"
        android:layout_width="match_parent"
        android:layout_height="240dp"
        android:background="@color/black">

        <androidx.media3.ui.PlayerView
            android:id="@+id/playerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:controller_layout_id="@layout/custom_player_control_view"
            app:hide_on_touch="true"
            app:resize_mode="fit"
            app:show_timeout="4000"
            app:use_controller="true" />
    </RelativeLayout>

    <!-- Bottom Half: Stream Channel Info & Channel List -->
    <LinearLayout
        android:id="@+id/layoutStreamDetails"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:padding="12dp">

        <!-- Active Playing Channel Header -->
        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            app:cardBackgroundColor="@color/bg_surface"
            app:cardCornerRadius="12dp"
            app:cardElevation="2dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:orientation="horizontal"
                android:padding="12dp">

                <ImageView
                    android:id="@+id/imgCurrentLogo"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:contentDescription="Channel Logo"
                    android:src="@drawable/img_app_logo" />

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="12dp"
                    android:layout_weight="1"
                    android:orientation="vertical">

                    <TextView
                        android:id="@+id/txtCurrentTitle"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Channel Title"
                        android:textColor="@color/text_primary"
                        android:textSize="16sp"
                        android:textStyle="bold" />

                    <TextView
                        android:id="@+id/txtCurrentCategory"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Live TV • HLS Stream"
                        android:textColor="@color/primary"
                        android:textSize="12sp" />
                </LinearLayout>

                <!-- Favorite Star Toggle -->
                <ImageView
                    android:id="@+id/btnFavToggle"
                    android:layout_width="38dp"
                    android:layout_height="38dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="Favorite Toggle"
                    android:padding="8dp"
                    android:src="@drawable/ic_favorite_border"
                    app:tint="@color/accent_gold" />

                <!-- Share Button -->
                <ImageView
                    android:id="@+id/btnShare"
                    android:layout_width="38dp"
                    android:layout_height="38dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="Share"
                    android:padding="8dp"
                    android:src="@drawable/ic_share"
                    app:tint="@color/text_primary" />

                <!-- Report Link -->
                <ImageView
                    android:id="@+id/btnReportLink"
                    android:layout_width="38dp"
                    android:layout_height="38dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="Report Broken Stream"
                    android:padding="8dp"
                    android:src="@drawable/ic_report"
                    app:tint="@color/accent_red" />
            </LinearLayout>
        </androidx.cardview.widget.CardView>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:text="More Live Channels &amp; Streams"
            android:textColor="@color/text_primary"
            android:textSize="15sp"
            android:textStyle="bold" />

        <!-- Channels RecyclerView -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvPlayerChannels"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
    </LinearLayout>

</LinearLayout>
